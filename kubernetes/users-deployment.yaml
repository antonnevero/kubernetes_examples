apiVersion: apps/v1
kind: Deployment
metadata:
  name: users-deployment
spec:
  replicas: 1
  selector: 
    matchLabels:
      app: users
  template:
    metadata:
      labels:
        app: users
    spec:
      containers:
        - name: users
          image: farnsworth1988/kub-demo-users:latest
          env:
            - name: AUTH_ADDRESS
              # value: "10.96.196.129"
              value: "auth-service.default"