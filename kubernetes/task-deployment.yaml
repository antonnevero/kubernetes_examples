apiVersion: apps/v1
kind: Deployment
metadata:
  name: tasks-deployment
spec:
  replicas: 1
  selector: 
    matchLabels:
      app: tasks
  template:
    metadata:
      labels:
        app: tasks
    spec:
      containers:
        - name: tasks
          image: farnsworth1988/kub-demo-task:latest
          env:
            - name: AUTH_ADDRESS
              # value: "10.96.196.129"
              value: "auth-service.default"
            - name: TASKS_FOLDER
              value: tasks